# Docker image publish to Google Artifact Registry (GAR), the dodo deploy plane.
#
# Mirrors the dodo-discord-bot publish workflow: publishing a GitHub Release
# builds the image, tags it with the release tag, and pushes it to GAR. This is
# the internal-deploy image. The public open-source image is built and pushed
# to GHCR by the `docker` job in ci.yml. The two are independent and target
# different registries.
#
# Authentication to GAR is keyless via Workload Identity Federation -- no secret
# is required. The job's `id-token: write` permission is what makes it work:
# without it GitHub issues no OIDC token and google-github-actions/auth fails
# before it ever reaches GCP. The service account is bound org-wide at
# `attribute.repository_owner/dodopayments`, so no GCP-side IAM change is needed.
#
# Prerequisites (repo settings, one-time, human):
#   - vars.REGION: GAR region host prefix, e.g. `us-central1`.
#   - vars.CHIMELY_GAR_LOCATION: full image path without a tag, e.g.
#     `us-central1-docker.pkg.dev/PROJECT/REPO/chimely`.
name: Docker Publish

on:
  release:
    types: [published]

concurrency: docker-publish-${{ github.ref }}

jobs:
  docker_build_and_push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      # Required for Workload Identity Federation.
      id-token: write
    env:
      IMAGE: ${{ vars.CHIMELY_GAR_LOCATION }}
      REGION: ${{ vars.REGION }}
      PRERELEASE: ${{ github.event.release.prerelease }}
    steps:
      - uses: actions/checkout@v4

      - name: Extract release tag
        run: echo "RELEASE_TAG=${GITHUB_REF#refs/tags/}" >> "$GITHUB_ENV"

      - name: Guard required repo variables
        run: |
          [ -n "$IMAGE" ]  || { echo "CHIMELY_GAR_LOCATION repo variable is unset" >&2; exit 1; }
          [ -n "$REGION" ] || { echo "REGION repo variable is unset" >&2; exit 1; }

      # Auth precedes the build so the registry cache can be read from GAR.
      - name: Google Auth
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: projects/1042125736866/locations/global/workloadIdentityPools/github-pool/providers/github-provider
          service_account: github-actions@dodopayments.iam.gserviceaccount.com

      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v2

      - name: Docker auth
        run: gcloud auth configure-docker "${REGION}-docker.pkg.dev"

      - uses: docker/setup-buildx-action@v3

      # `latest` tracks the newest stable release, so a pre-release publishes its
      # own tag but never moves `latest`.
      - name: Compute image tags
        id: tags
        run: |
          TAGS="$IMAGE:$RELEASE_TAG"
          [ "$PRERELEASE" = "true" ] || TAGS="$TAGS,$IMAGE:latest"
          echo "tags=$TAGS" >> "$GITHUB_OUTPUT"

      # Durable registry cache in GAR. The cargo-chef dependency layer is
      # compiled once and reused across releases.
      - name: Build and push chimely image
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: ${{ steps.tags.outputs.tags }}
          cache-from: type=registry,ref=${{ env.IMAGE }}:buildcache
          cache-to: type=registry,ref=${{ env.IMAGE }}:buildcache,mode=max
